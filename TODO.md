# TODO

Current follow-ups only. Put completed work in the Git history, not here.

- [ ] Verify the deployed iPhone shortcut calls the current `quick-note` endpoint; correct or retire the old `quik-note` deployment if it remains.
- [ ] Confirm `VITE_VAPID_PUBLIC_KEY` is configured in production, then remove the misleading comment in `src/services/notifications/push.service.ts`.
