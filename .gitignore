# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Dexie Cloud keys (sensitive)
dexie-cloud.key

# Environment files (keep .env.example)
.env
.env.local
.env.production

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Windows null device file created by shell redirections
nul

# MCP configuration with database credentials
.mcp.json

# Playwright MCP temp files
.playwright-mcp/
test-results/
playwright-report/
blob-report/

# Supabase CLI temp files
supabase/.temp/

# Claude Code per-"part" progress journals (local scratch, resumable)
.claude/progress/

# Active-part quick-notes scratchpad (local, OneDrive-synced, fed by /note + iPhone shortcut)
journal-notes.md

# git worktrees (isolated workspaces; never tracked)
.claude/worktrees/
